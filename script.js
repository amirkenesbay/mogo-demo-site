'use strict';

// тип_переменной название_переменной = значение_переменной
// prompt(текст, стандартное значение);

let width = null;
let height = null;
let area = (height ?? 100) * (width ?? 50);
alert(area);
